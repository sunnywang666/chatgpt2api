# Copy to a private file, fill the bearer token, and keep that file out of Git.
# This is the planned public address. It is not yet deployed or live-validated.
# Do not put /v1 at the end; the client uses both /v1 and /api routes.
SERVER_ROOT=https://app.hugsweetglobal.com/ai
CHATGPT2API_BEARER_TOKEN=

# Optional. The command-line --state option takes precedence.
IMAGE_CLIENT_STATE=
